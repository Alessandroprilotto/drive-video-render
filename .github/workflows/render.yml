name: render_video

on:
  workflow_dispatch:
  repository_dispatch:
    types: [render_video]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Show event
        run: |
          echo "EVENT=$GITHUB_EVENT_NAME"
          cat "$GITHUB_EVENT_PATH" || true
